---
applications:
- name: tetris-ortools-app   # <-- ¡Elige un nombre único!
  memory: 1G                  # Empezamos con 1GB por ortools/pandas. Puedes ajustarlo luego.
  random-route: true
  buildpacks:
  - python_buildpack
  command: gunicorn -w 4 -k uvicorn.workers.UvicornWorker backend.main:app